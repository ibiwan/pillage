import React from 'react';

import BaseIcon from './BaseIcon';

export default (props) => (
  // eslint-disable-next-line react/jsx-props-no-spreading
  <BaseIcon {...props} name="pencil" type="font-awesome" />
);
